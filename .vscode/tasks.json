{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "CMake Build",
      "type": "shell",
      "command": "cmake",
      "args": [
        "-S", "${workspaceFolder}",
        "-B", "${workspaceFolder}/build"
      ],
      "group": "build"
    },
    {
      "label": "CMake Compile",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build", "${workspaceFolder}/build"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "dependsOn": ["CMake Build"]
    }
  ]
}


// 第一步 (CMake Build)：生成 build/ 目录里的构建系统文件（类似 Makefile 或 ninja.build）。

// 第二步 (CMake Compile)：真正执行编译。

// 设置 dependsOn，保证按顺序执行。

// 这样 Ctrl+Shift+B 就能自动执行 CMake 构建，不和插件冲突。
